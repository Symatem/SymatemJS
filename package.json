{
  "name": "SymatemWasm",
  "version": "0.0.0-semantic-release",
  "authors": [
    "Alexander Meißner"
  ],
  "engines": {
    "node": ">=8.0"
  },
  "description": "Symatem WebAssembly API",
  "main": "dist/main.js",
  "module": "src/main.js",
  "dependencies": {},
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "nyc": "^10.1.2",
    "rollup": "^0.41.4",
    "semantic-release": "^6.3.6"
  },
  "scripts": {
    "cover": "nyc npm test",
    "test": "mocha tests/*_test.js",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post",
    "prepublish": "rollup --output=dist/main.js -c -- src/main.js",
    "pretest": "npm run prepublish",
    "precover": "npm run prepublish"
  },
  "nyc": {
    "include": [
      "dist/**/*.js"
    ],
    "reporter": [
      "lcov"
    ],
    "report-dir": "./coverage"
  }
}
